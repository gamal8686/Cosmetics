import 'package:flutter/material.dart';

class idpage extends StatelessWidget {
  const idpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('idpage')));
  }
}
